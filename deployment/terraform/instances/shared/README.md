This stack contains the ECR repository definition in the shared account.

Deployment is done locally via terraform - ensure you are authenticated with the `emnify-shared` account 

- `terraform init`
- `terraform plan`
- `terraform apply`
